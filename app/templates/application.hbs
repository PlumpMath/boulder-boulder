<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1>{{link-to 'Boulder Boulder' 'index'}}</h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">
      {{#if session.gym}}
        <li>{{link-to 'add problem' 'problems.new'}}</li>
      {{/if}}
      <li class="has-dropdown">
        <a href="#">username</a>
        <ul class="dropdown">
          <li><a href="#">First link in dropdown</a></li>
        </ul>
      </li>
    </ul>

    <ul class="left">
      {{#if session.gym }}
        <li>{{link-to session.gym.name 'problems'}}</li>
      {{/if}}
    </ul>
  </section>
</nav>

<div class="container">
  {{outlet}}
</div>
